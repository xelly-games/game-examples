import {vec} from 'excalibur';

export const Config = {
    PickerLetterPadding: vec(16, 2),
    PickerLetterSpacingX: 4,
    PickerLetterSpacingY: 8,
    PhraseLineSpacingY: 14,
    PhraseMarginY: 10,
    GallowsLeftMargin: 10,
} as const;
